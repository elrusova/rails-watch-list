<div class="container mb-3">
  <h2 class="mt-3">Add a new list</h2>
  <%= simple_form_for [@list] do |f| %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>
  <div class="container mt-4" style="padding-left: 0">
    <%= link_to 'Back to list', lists_path %>
  </div>
</div>
